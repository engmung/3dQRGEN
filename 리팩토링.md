🏗️ 종합 리팩토링 계획 (Comprehensive Refactoring Plan)
6개의 병렬 분석 에이전트가 전체 코드베이스를 검토한 결과를 종합했습니다.
📊 현황 분석 요약
발견된 주요 문제점
보안 취약점: 1건 (클라이언트 측 가격 계산)
성능 문제: 5건 (React 규칙 위반, 메모리 누수 등)
중복 코드: ~800줄 (전체 코드의 약 15%)
DB 스키마 문제: 8건 (미사용 필드, 타임존 불일치 등)
타입 안전성: 12건 (any 타입 사용)
🎯 리팩토링 전략: 4 Phase 접근
Phase 1: Critical Fixes (보안 & 성능) ⚠️
시간: 4-6시간 | 우선순위: 🔴 URGENT
1.1 보안 수정 (Security)
클라이언트 측 가격 계산 제거
frontend/src/utils/pricing.ts → 백엔드로 이동
backend/app/services/pricing_service.py 생성
모든 주문 API에서 가격 검증 추가
1.2 성능 크리티컬 수정
Scene3D.tsx 수정 (React 규칙 위반)
Lines 94-138: useLoader 훅을 컴포넌트 최상위로 이동
모델 로딩 로직을 useMemo로 감싸기
메모리 누수 방지
37개의 .clone() 호출에 대한 cleanup 로직 추가
useEffect cleanup에서 geometry/material dispose 추가
Phase 2: Database Schema Cleanup 🗄️
시간: 2-3시간 | 우선순위: 🔴 HIGH
2.1 타임존 통일

# 수정 파일:

# - backend/app/models/product.py (Lines 21-22)

# - backend/app/models/pricing.py (Line 18)

# Before: datetime.utcnow

# After: get_kst_now (다른 모델과 일관성)

2.2 미사용 필드 제거

# backend/app/models/order.py

- download_token (Line 43) # 완전 미사용
- payment_id (Line 39) # 구현 안 됨

# backend/app/models/order_group.py

# backend/app/models/order_line_item.py

- production_date 인덱스 제거 (deprecated 필드)
  2.3 Foreign Key 추가

# backend/app/models/order_line_item.py (Line 28)

product_sku = Column(
String(50),
ForeignKey('products.sku'), # 🆕 추가
nullable=False,
index=True
)
2.4 유틸리티 공통화

# 새 파일 생성: backend/app/utils/datetime_utils.py

def get_kst_now() -> datetime:
"""KST 현재 시각 반환 (모든 모델에서 공통 사용)"""
return datetime.now(pytz.timezone('Asia/Seoul'))
Phase 3: Backend Service Layer Refactoring 🔧
시간: 6-8시간 | 우선순위: 🟡 MEDIUM
3.1 서비스 레이어 분리
새로 생성할 파일들:
backend/app/services/
├── pricing_service.py # 가격 계산 로직
├── file_service.py # 파일/ZIP 관리 통합
├── production_service.py # 생산 일정 관리
├── order_service.py # 주문 비즈니스 로직
└── validators.py # 공통 검증 로직
3.1.1 pricing_service.py
class PricingService:
@staticmethod
def calculate_product_price(
customization_data: dict,
pricing: PricingSetting
) -> float:
"""현재 routers/orders.py와 order_groups.py에 중복된 로직""" # 통합된 가격 계산

    @staticmethod
    def get_pricing_settings(
        db: Session,
        create_if_missing: bool = False
    ) -> PricingSetting:
        """현재 3곳에서 중복된 로직"""

3.1.2 file_service.py (storage.py 확장)

# storage.py에 추가할 함수들

async def create_order_group_zip(group_uuid: str, customer_name: str) -> str:
"""현재 order_groups.py Lines 478-513에 있는 로직"""

async def create_line_item_zip(group_uuid: str, line_item_uuid: str) -> str:
"""현재 order_groups.py Lines 562-583에 있는 로직"""

async def delete_order_files(order_uuid: str):
"""현재 orders.py Lines 322-335에 있는 로직"""

def find_model_files(directory: str) -> tuple[list[str], list[str]]:
"""현재 3곳에서 중복된 glob 로직"""
3.1.3 production_service.py (production_allocator.py 확장)
class ProductionScheduleService:
@staticmethod
def allocate_dates(...):
"""기존 allocate_production_dates"""

    @staticmethod
    def deallocate_dates(order_group: OrderGroup, db: Session):
        """현재 order_groups.py Lines 420-438에 있는 로직"""

    @staticmethod
    def validate_capacity(required: int, schedule: ProductionSchedule):
        """새로운 검증 로직"""

3.2 Constants 파일 생성

# backend/app/constants.py

class OrderStatus:
PENDING = "pending"
PAID = "paid"
COMPLETED = "completed"
FAILED = "failed"

    @classmethod
    def all(cls) -> list[str]:
        return [cls.PENDING, cls.PAID, cls.COMPLETED, cls.FAILED]

PRICE_TOLERANCE = 1.0
DEFAULT_PRODUCTION_DAYS = 60
3.3 거대 함수 분리
order_groups.py의 create_order_group() (Lines 93-313, 220줄)

# 현재: 단일 220줄 함수

# 목표: 10개의 명확한 함수로 분리

async def create_order_group(...):
line_items_data = parse_line_items_json(line_items_json)
validate_line_items(line_items_data)

    allocation = await allocate_production(line_items_data, db)
    group = create_group_record(customer_data, db)

    for item_data in line_items_data:
        await process_single_line_item(item_data, group, allocation, files, db)

    finalize_pricing(group, db)
    await notify_telegram(group)

    return build_response(group)

Phase 4: Frontend Refactoring ⚛️
시간: 8-10시간 | 우선순위: 🟡 MEDIUM
4.1 공통 유틸리티 생성
새로 생성할 파일들:
frontend/src/
├── utils/
│ ├── qrHelpers.ts # QR 타입 라벨 매핑
│ ├── orderHelpers.ts # 주문 상태 변환
│ ├── clipboard.ts # 클립보드 복사
│ └── materialFactory.ts # Three.js Material 생성
│
├── styles/
│ ├── colors.ts # 색상 상수
│ ├── spacing.ts # 간격/z-index 상수
│ ├── buttonStyles.ts # 버튼 스타일
│ └── modalStyles.ts # 모달 스타일
│
├── components/common/
│ ├── Modal.tsx # 재사용 모달
│ ├── BaseCalendar.tsx # 달력 베이스
│ └── DataLoader.tsx # 로딩/에러 상태
│
└── hooks/
├── usePersistedState.ts # LocalStorage 훅
└── useOrders.ts # 주문 API 훅
4.2 스타일 상수 추출
colors.ts
export const COLORS = {
status: {
pending: '#ffc107',
paid: '#28a745',
completed: '#007bff',
failed: '#dc3545',
},
primary: '#4CAF50',
danger: '#dc3545',
// ... 현재 20+ 곳에 하드코딩된 색상 통합
};
buttonStyles.ts
export const BUTTON_PRIMARY = {
padding: '10px 20px',
backgroundColor: '#4CAF50',
color: 'white',
border: 'none',
borderRadius: '4px',
cursor: 'pointer',
};
// ... 현재 40+ 번 중복된 버튼 스타일 통합
4.3 대형 컴포넌트 분리
Admin.tsx (980 lines) → 4개 컴포넌트
Admin.tsx (200 lines)
├── components/admin/PricingSettingsPanel.tsx (110 lines)
├── components/admin/OrderTable.tsx (180 lines)
└── components/admin/OrderDetailModal.tsx (350 lines)
AddressForm.tsx (555 lines) → 3개 컴포넌트
AddressForm.tsx (150 lines)
├── components/address/CustomerInfoFields.tsx (150 lines)
├── components/address/PaymentInfo.tsx (180 lines)
└── components/address/PostalCodeModal.tsx (35 lines)
4.4 OBJ Export 로직 통합
현재 문제:
objExporter.ts: 4개의 다른 export 함수
objGenerator.ts: 중복된 OBJ/MTL 생성 로직
통합 방안:
// objExporter.ts에 단일 export 함수
export function exportToOBJ(options: {
source: 'scene' | 'collected' | 'glb';
meshes?: THREE.Mesh[];
rotation?: THREE.Euler;
download?: boolean;
}): Blob | void {
// 통합된 로직
}
4.5 GLB 로딩 캐싱
// glbLoader.ts 개선
const regionCache = new Map<string, GLBRegions>();

export async function loadGLBRegions(glbPath: string): Promise<GLBRegions> {
if (regionCache.has(glbPath)) {
return regionCache.get(glbPath)!; // 캐시 히트
}
// ... 로딩 로직
regionCache.set(glbPath, regions);
return regions;
}
🤖 병렬 처리 계획 (Sub-Agent Distribution)
Agent 1: Backend Models & DB
담당 파일:

- backend/app/models/\*.py (6 files)
- backend/app/database.py
- backend/app/utils/datetime_utils.py (신규)

작업:

1. get_kst_now() 공통화
2. 타임존 통일 (product.py, pricing.py)
3. 미사용 필드 제거 (download_token, payment_id)
4. production_date 인덱스 제거
5. Foreign Key 추가 (product_sku)
6. 마이그레이션 스크립트 작성
   Agent 2: Backend Services (1/2)
   담당 파일:

- backend/app/services/pricing_service.py (신규)
- backend/app/services/file_service.py (storage.py 확장)
- backend/app/constants.py (신규)

작업:

1. pricing_service.py 생성 (가격 계산 통합)
2. file_service.py에 ZIP/파일 관리 함수 추가
3. constants.py 생성 (OrderStatus, PRICE_TOLERANCE 등)
4. 유틸리티 함수 추출 (filename sanitization 등)
   Agent 3: Backend Services (2/2) & Routers
   담당 파일:

- backend/app/services/production_service.py (신규)
- backend/app/routers/order_groups.py
- backend/app/routers/orders.py

작업:

1. production_service.py 생성 (deallocate 로직 추가)
2. create_order_group() 함수 분리
3. 중복 코드 제거 (serialization, status validation)
4. 서비스 레이어 호출로 변경
   Agent 4: Frontend Utils & Styles
   담당 파일:

- frontend/src/utils/qrHelpers.ts (신규)
- frontend/src/utils/orderHelpers.ts (신규)
- frontend/src/styles/\*.ts (신규 4개 파일)
- frontend/src/utils/objExporter.ts (통합)

작업:

1. 유틸리티 함수 추출 (getQRTypeLabel, getStatusText 등)
2. 색상/스타일 상수 생성
3. OBJ export 로직 통합
4. materialFactory.ts 생성
   Agent 5: Frontend Components (Common & 3D)
   담당 파일:

- frontend/src/components/common/\*.tsx (신규 3개)
- frontend/src/components/Scene3D.tsx (수정)
- frontend/src/components/QRPlateInstance.tsx (수정)
- frontend/src/utils/glbLoader.ts (캐싱 추가)

작업:

1. Modal.tsx, BaseCalendar.tsx, DataLoader.tsx 생성
2. Scene3D.tsx useLoader 수정 (React 규칙 준수)
3. QRPlateInstance.tsx memoization 추가
4. GLB 로딩 캐싱 구현
5. geometry cleanup 추가
   Agent 6: Frontend Pages (Admin & Forms)
   담당 파일:

- frontend/src/pages/Admin.tsx (분리)
- frontend/src/components/AddressForm.tsx (분리)
- frontend/src/components/admin/\*.tsx (신규 3개)
- frontend/src/components/address/\*.tsx (신규 3개)

작업:

1. Admin.tsx → 4개 컴포넌트로 분리
2. AddressForm.tsx → 3개 컴포넌트로 분리
3. 공통 스타일/유틸 적용
4. Props drilling 제거 (store 활용)
   📋 리팩토링 규칙 (Refactoring Rules)
   ✅ DO (해야 할 것)
   단일 책임 원칙: 한 함수/파일은 한 가지 일만
   DRY 원칙: 3번 이상 반복되면 공통화
   타입 안전성: any 타입 제거, 명확한 인터페이스 정의
   성능 우선: useMemo, useCallback, cleanup 함수 추가
   보안 우선: 백엔드에서 모든 비즈니스 로직/검증 수행
   테스트 가능성: 작은 순수 함수로 분리
   문서화: 복잡한 로직에 주석 추가
   ❌ DON'T (하지 말아야 할 것)
   기능 변경 금지: 동작은 동일하게 유지
   새 기능 추가 금지: 리팩토링만 집중
   과도한 추상화 금지: 실제 재사용되는 것만 추출
   임의 파일명 변경 금지: 명확한 이유 없이 이름 변경 X
   DB 데이터 손실 금지: 마이그레이션 스크립트 필수
   테스트되지 않은 배
