# 3D QR 플랫폼 - 프론트엔드 구현 현황

## 📋 프로젝트 개요
React + Three.js 기반의 3D QR 코드 커스터마이징 플랫폼 프론트엔드

## ✅ 구현 완료 기능

### 1. 3D QR 코드 생성
- **방식**: QR 코드를 실제 3D 블록 지오메트리로 생성 (텍스처 매핑 방식이 아님)
- **프로세스**:
  1. `qrcode` 라이브러리로 QR 비트맵 데이터 생성
  2. 각 검은 픽셀마다 개별 BoxGeometry 생성
  3. BufferGeometryUtils로 모든 박스를 하나의 지오메트리로 병합
- **구현 파일**:
  - `frontend/src/utils/qrUtils.ts` - QR 비트맵 생성
  - `frontend/src/components/QRPlate.tsx` - 3D 지오메트리 생성

### 2. 판때기 및 QR 커스터마이징
- **판 크기 조정**:
  - 너비 (mm)
  - 높이 (mm)
  - 두께 (mm)
- **QR 설정**:
  - 크기 (mm)
  - 두께 (mm)
  - Y축 오프셋 (판 상단에서의 거리, mm)
- **기본값**:
  - 판: 70mm × 140mm × 2mm (핸드폰 크기)
  - QR: 50mm × 50mm × 2mm
  - QR 위치: 상단에서 10mm 아래
  - 기본 URL: `https://example.com`
- **단위 변환**: 1mm = 1 Three.js unit
- **구현 파일**:
  - `frontend/src/store/useDesignStore.ts` - Zustand 상태 관리
  - `frontend/src/components/SidePanel.tsx` - UI 컨트롤

### 3. STL 파일 출력
- **기능**: 3D 프린팅용 STL 파일 다운로드
- **3D 프린팅 최적화**:
  - Z축 180° 회전 적용
  - QR이 없는 평평한 면이 프린터 베드 쪽으로 향하도록 자동 조정
  - 서포트 없이 출력 가능한 방향으로 설정
- **파일명**: `qr-plate-{timestamp}.stl` (바이너리 형식)
- **구현 파일**:
  - `frontend/src/utils/stlExporter.ts` - STL 출력 로직
  - `frontend/src/components/QRPlate.tsx` - forwardRef로 exportSTL 메서드 노출

### 4. 3D 렌더링 및 시각화
- **렌더링 엔진**: React Three Fiber (@react-three/fiber)
- **조명**:
  - AmbientLight (강도 0.6)
  - DirectionalLight 2개 (주 조명 0.8, 보조 0.3)
  - 주 조명에 그림자 매핑 활성화 (2048×2048 해상도)
- **그림자**:
  - QR 블록이 판에 그림자를 드리워 깊이감 표현
  - 판: 그림자 받기만 (receiveShadow)
  - QR 블록: 그림자 투사만 (castShadow)
- **카메라**:
  - 위치: [200, 150, 200]
  - FOV: 50°
- **컨트롤**: OrbitControls (회전, 줌, 팬)
  - 최소 거리: 50 units
  - 최대 거리: 500 units
  - 댐핑 활성화
- **재질**:
  - 판: 흰색 MeshStandardMaterial (#ffffff)
  - QR 블록: 검은색 MeshStandardMaterial (#000000)
- **구현 파일**: `frontend/src/components/Scene3D.tsx`

### 5. UI 레이아웃
- **구조**: 단일 페이지, 좌우 분할 레이아웃
  - 좌측: 전체 화면 3D 씬 (flex: 1)
  - 우측: 사이드 패널 (고정 너비 300px)
- **사이드 패널 요소**:
  - URL 입력창
  - 판 크기 입력 (너비/높이/두께)
  - QR 설정 입력 (크기/두께/Y오프셋)
  - STL 다운로드 버튼
  - 디버그용 QR 2D 미리보기 (Canvas)
- **구현 파일**:
  - `frontend/src/App.tsx` - 메인 레이아웃
  - `frontend/src/components/SidePanel.tsx` - 컨트롤 패널

## 🛠️ 기술 스택

### 코어
- **Vite** - 빌드 툴
- **React 18** + **TypeScript** - UI 프레임워크

### 3D 그래픽
- **React Three Fiber** (@react-three/fiber) - React용 Three.js 렌더러
- **@react-three/drei** - R3F 헬퍼 컴포넌트 (OrbitControls 등)
- **Three.js** - 3D 그래픽 라이브러리
  - BufferGeometryUtils (지오메트리 병합)
  - STLExporter (STL 파일 출력)

### 상태 관리 및 유틸리티
- **Zustand** - 경량 상태 관리
- **qrcode** - QR 코드 비트맵 생성
- **@types/qrcode** - TypeScript 타입 정의

## 📁 파일 구조

```
frontend/
├── src/
│   ├── components/
│   │   ├── QRPlate.tsx          # 3D QR 판 생성 (forwardRef, exportSTL)
│   │   ├── Scene3D.tsx          # Canvas, 조명, OrbitControls 설정
│   │   └── SidePanel.tsx        # 우측 컨트롤 패널 UI
│   ├── store/
│   │   └── useDesignStore.ts    # Zustand 전역 상태 (설정값 관리)
│   ├── utils/
│   │   ├── qrUtils.ts           # QR 비트맵 생성 함수
│   │   └── stlExporter.ts       # STL 파일 출력 함수
│   ├── App.tsx                  # 메인 레이아웃 (좌우 분할)
│   └── main.tsx                 # React 엔트리 포인트
├── package.json
└── vite.config.ts
```

## 🔧 주요 컴포넌트 설명

### QRPlate.tsx
- **역할**: 판과 QR 블록 3D 지오메트리 생성
- **패턴**: forwardRef + useImperativeHandle로 부모에 exportSTL() 노출
- **주요 로직**:
  - QR 비트맵 → 개별 BoxGeometry 생성 → mergeGeometries로 병합
  - 판 회전: X축 90° (눕힘)
  - QR 위치: Y축 오프셋 적용

### Scene3D.tsx
- **역할**: 3D 렌더링 환경 구성
- **주요 설정**:
  - Canvas shadows 활성화
  - DirectionalLight shadow-mapSize, shadow-camera 설정
  - OrbitControls 범위 제한

### SidePanel.tsx
- **역할**: 사용자 입력 UI
- **기능**:
  - Zustand store 연결 (useDesignStore)
  - 각 설정값 입력 필드
  - STL 다운로드 버튼 (URL 없으면 비활성화)
  - 2D QR 미리보기 (디버깅용)

### useDesignStore.ts
- **역할**: 전역 상태 관리
- **상태**:
  - qrUrl, plateWidth, plateHeight, plateDepth
  - qrSize, qrDepth, qrYOffset
- **액션**: 각 값의 setter 함수

## 📋 향후 개발 예정 기능

### 단기
- [ ] UI/UX 개선 및 스타일링 (디자인 시스템 적용)
- [ ] 수치 입력 개선 (슬라이더, 단위 표시 등)
- [ ] 입력값 유효성 검증
- [ ] 로딩 상태 표시

### 중기
- [ ] 3D 장면 추가 옵션 (배경, 그리드 등)
- [ ] 거치대 3D 모델 추가
- [ ] 거치대에 맞는 QR 위치 자동 조정
- [ ] 재질/색상 커스터마이징
- [ ] 프리셋 저장/불러오기

### 장기
- [ ] 백엔드 연동 (STL 파일 서버 저장)
- [ ] 사용자 계정 및 프로젝트 관리
- [ ] 고급 3D 편집 기능
- [ ] 실시간 협업 기능

## 🐛 알려진 이슈 및 해결 내역

### 해결됨
- ✅ QR 텍스처 방식 실패 → 3D 블록 방식으로 변경
- ✅ TypeScript import 에러 (QRPlateRef, Scene3DRef) → type import로 수정
- ✅ 크기 10배 작게 나옴 → MM_TO_UNITS 0.1 → 1로 수정
- ✅ QR 블록 깊이감 없음 → 그림자 렌더링 추가
- ✅ qrcode 타입 정의 없음 → @types/qrcode 설치
- ✅ modules.get() 타입 불일치 → !! 연산자로 boolean 변환

## 🚀 실행 방법

```bash
# 의존성 설치
cd frontend
npm install

# 개발 서버 실행
npm run dev

# 빌드
npm run build
```

## 📝 개발 노트

- **좌표계**: Three.js 기본 좌표계 사용 (Y-up)
- **회전**: 판을 X축 90° 회전으로 눕혀서 표시
- **STL 출력 시**: Z축 180° 추가 회전으로 프린팅 방향 조정
- **성능**: BufferGeometryUtils로 QR 블록 병합하여 드로우콜 최소화
- **디버깅**: SidePanel에 2D QR 미리보기로 QR 생성 확인 가능
