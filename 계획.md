3D QR 거치대 커스터마이징 플랫폼 - 핵심 기능 정리

1. 프론트엔드 (React + R3F)
   필요한 기능

거치대 선택

거치대 목록 표시
선택 시 3D 프리뷰 로드

QR 생성

URL 입력
QR 코드 자동 생성 (qrcode.react)

커스터마이징 패널

텍스트 추가 (내용, 위치, 크기)
로고 업로드 (위치, 크기)
QR 크기/위치 조절
판 두께/모양 선택

3D 프리뷰 (R3F)

거치대 베이스 모델 표시
QR 판 실시간 렌더링
회전/줌 컨트롤

STL Export

QR 판만 STL 파일로 변환
Binary STL 생성

주문 폼

고객 정보 입력 (이메일, 이름, 주소)
백엔드로 데이터 전송
Lemon Squeezy 결제 페이지로 이동

2. 백엔드 (FastAPI + 라즈베리파이)
   필요한 기능

거치대 관리

GET /api/stands - 거치대 목록 반환
GET /api/stands/{id}/model - 3D 모델 파일 제공

주문 처리

POST /api/orders

STL 파일 수신 & 저장
stand_id, 커스터마이징 정보 저장
주문 DB 등록
Lemon Squeezy 결제 URL 생성 & 반환

결제 Webhook

POST /api/webhooks/lemonsqueezy

결제 완료 알림 수신
주문 상태 업데이트
다운로드 링크 이메일 발송

파일 다운로드

GET /api/downloads/{order_id}/{token}

토큰 검증
STL 파일 제공

파일 저장

로컬 스토리지에 주문별 폴더 생성
qr_plate.stl, stand_id.txt, order_info.json 저장

이메일 발송

결제 완료 시 다운로드 링크 전송

3. 데이터베이스
   필요한 테이블

stands

id, name, thumbnail, preview_model_url, price

orders

id, stand_id, qr_url, customization(JSON), customer_info(JSON), status, payment_id, download_token

4. 기술 스택 요약
   프론트엔드:

React + Vite
React Three Fiber + Drei
qrcode.react
Zustand
TailwindCSS

백엔드:

FastAPI
SQLite (또는 PostgreSQL)
python-multipart
httpx (Lemon Squeezy API)
smtplib (이메일)

배포:

프론트: Vercel/Netlify
백엔드: 라즈베리파이 + Nginx + SSL

5. 데이터 흐름
   사용자 → 거치대 선택 → QR 입력 → 커스터마이징
   → 3D 프리뷰 → 주문 버튼 → STL 생성 → 백엔드 전송
   → 결제 페이지 → 결제 완료 → Webhook → 이메일 발송
   → 다운로드 링크 → STL 다운로드 → 프린팅
