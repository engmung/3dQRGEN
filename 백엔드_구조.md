# 3D QR 플랫폼 - 백엔드 구조 문서

## 📋 프로젝트 개요
FastAPI + SQLite 기반의 3D QR 코드 플랫폼 백엔드 API

## 🏗️ 프로젝트 구조

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI 앱 엔트리포인트
│   ├── config.py               # 환경설정 (Pydantic Settings)
│   ├── database.py             # SQLite 연결 및 세션 관리
│   │
│   ├── models/                 # SQLAlchemy 모델
│   │   ├── __init__.py
│   │   ├── stand.py            # Stand 테이블
│   │   └── order.py            # Order 테이블
│   │
│   ├── schemas/                # Pydantic 스키마 (요청/응답)
│   │   ├── __init__.py
│   │   ├── stand.py            # Stand 스키마
│   │   └── order.py            # Order 스키마
│   │
│   ├── routers/                # API 라우터
│   │   ├── __init__.py
│   │   ├── stands.py           # GET /api/stands
│   │   ├── orders.py           # POST /api/orders
│   │   ├── webhooks.py         # POST /api/webhooks/lemonsqueezy
│   │   └── downloads.py        # GET /api/downloads/{order_uuid}/{token}
│   │
│   ├── services/               # 비즈니스 로직
│   │   ├── __init__.py
│   │   ├── payment.py          # Lemon Squeezy API (TODO)
│   │   ├── email.py            # 이메일 발송 (TODO)
│   │   └── storage.py          # 파일 저장/관리 (TODO)
│   │
│   └── utils/                  # 유틸리티
│       ├── __init__.py
│       └── token.py            # JWT 토큰 생성/검증 (TODO)
│
├── storage/                    # STL 파일 저장소
│   └── orders/
│       └── {order_uuid}/
│           ├── qr_plate.stl
│           └── order_info.json
│
├── static/                     # 정적 파일 (거치대 3D 모델, 썸네일)
│   └── stands/
│       ├── 1_preview.stl
│       └── 1_thumbnail.png
│
├── data/
│   └── qr_platform.db          # SQLite 데이터베이스
│
├── requirements.txt            # Python 의존성
├── .env.example                # 환경변수 템플릿
└── .env                        # 환경변수 (gitignore)
```

## 📊 데이터베이스 스키마

### stands 테이블
```sql
CREATE TABLE stands (
    id INTEGER PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    thumbnail_url VARCHAR(255),      -- /static/stands/{id}_thumbnail.png
    preview_model_url VARCHAR(255),  -- /static/stands/{id}_preview.stl
    price FLOAT NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### orders 테이블
```sql
CREATE TABLE orders (
    id INTEGER PRIMARY KEY,
    order_uuid VARCHAR(36) UNIQUE NOT NULL,  -- 공개용 UUID
    stand_id INTEGER REFERENCES stands(id),

    -- QR & Customization
    qr_url VARCHAR(500) NOT NULL,
    customization JSON,  -- {plate_width, plate_height, plate_depth, qr_size, qr_depth, qr_y_offset}

    -- Customer Info
    customer_email VARCHAR(255) NOT NULL,
    customer_name VARCHAR(100),
    customer_address TEXT,

    -- Payment & Status
    status VARCHAR(20) DEFAULT 'pending',  -- pending, paid, completed, failed
    payment_id VARCHAR(100),  -- Lemon Squeezy order ID

    -- Download
    download_token VARCHAR(255),
    stl_file_path VARCHAR(255),

    -- Timestamps
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## 🔌 API 엔드포인트

### 거치대 관리

#### `GET /api/stands`
거치대 목록 조회

**Query Parameters:**
- `skip`: 페이지네이션 오프셋 (기본: 0)
- `limit`: 조회 개수 (기본: 100)

**Response:**
```json
[
  {
    "id": 1,
    "name": "기본 거치대",
    "description": "심플한 디자인의 기본 거치대",
    "thumbnail_url": "/static/stands/1_thumbnail.png",
    "preview_model_url": "/static/stands/1_preview.stl",
    "price": 15000
  }
]
```

#### `GET /api/stands/{stand_id}`
거치대 상세 조회

**Response:** 단일 거치대 객체

---

### 주문 처리

#### `POST /api/orders`
주문 생성

**Content-Type:** `multipart/form-data`

**Request Body:**
```
stand_id: 1
qr_url: "https://example.com"
customization: '{"plate_width": 70, "plate_height": 140, "plate_depth": 2, "qr_size": 50, "qr_depth": 2, "qr_y_offset": 10}'
customer_email: "user@example.com"
customer_name: "홍길동"
customer_address: "서울시 강남구..."
stl_file: [binary STL file]
```

**Response:**
```json
{
  "order_uuid": "123e4567-e89b-12d3-a456-426614174000",
  "payment_url": "https://checkout.lemonsqueezy.com/..."
}
```

**처리 과정:**
1. STL 파일 저장 → `storage/orders/{order_uuid}/qr_plate.stl`
2. DB에 주문 레코드 생성 (status: pending)
3. Lemon Squeezy API 호출 → 결제 URL 생성
4. 결제 URL 반환

---

### 결제 웹훅

#### `POST /api/webhooks/lemonsqueezy`
Lemon Squeezy 결제 완료 알림

**Headers:**
- `X-Signature`: 웹훅 서명 (HMAC SHA256)

**Request Body:** Lemon Squeezy 웹훅 페이로드

**처리 과정:**
1. 웹훅 서명 검증
2. 주문 상태 업데이트 (pending → paid)
3. JWT 다운로드 토큰 생성 (72시간 유효)
4. 다운로드 링크 이메일 발송

---

### 파일 다운로드

#### `GET /api/downloads/{order_uuid}/{token}`
STL 파일 다운로드

**Path Parameters:**
- `order_uuid`: 주문 UUID
- `token`: JWT 다운로드 토큰

**Response:** STL 파일 (`Content-Disposition: attachment`)

**처리 과정:**
1. JWT 토큰 검증 (만료, order_uuid 일치)
2. 주문 조회 및 파일 경로 확인
3. FileResponse로 STL 파일 전송

---

### 헬스체크

#### `GET /`
API 상태 확인

**Response:**
```json
{
  "message": "3D QR Platform API",
  "status": "running",
  "version": "1.0.0"
}
```

#### `GET /health`
헬스체크

**Response:**
```json
{
  "status": "healthy"
}
```

## 🧩 핵심 모듈 설명

### `app/config.py`
- Pydantic Settings 기반 환경변수 관리
- `.env` 파일 자동 로드
- `settings` 싱글톤 인스턴스 제공

**주요 설정:**
- `database_url`: SQLite DB 경로
- `lemon_squeezy_*`: Lemon Squeezy API 설정
- `smtp_*`: 이메일 발송 설정
- `secret_key`: JWT 서명 키
- `cors_origins`: CORS 허용 도메인

### `app/database.py`
- SQLAlchemy 엔진 및 세션 관리
- `get_db()`: 의존성 주입용 DB 세션 생성기
- `init_db()`: 테이블 자동 생성

### `app/models/`
- SQLAlchemy ORM 모델 정의
- `Stand`: 거치대 모델
- `Order`: 주문 모델
- Relationship 정의 (Stand ↔ Order)

### `app/schemas/`
- Pydantic 스키마 (요청/응답 검증)
- `StandResponse`: 거치대 API 응답
- `OrderCreate`: 주문 생성 요청
- `OrderResponse`: 주문 생성 응답
- `Customization`: 커스터마이징 설정

### `app/routers/`
- FastAPI 라우터
- 각 도메인별 엔드포인트 정의
- 현재 기본 구조만 구현 (TODO 주석으로 표시)

## 🛠️ 기술 스택

### 프레임워크
- **FastAPI 0.104.0** - 고성능 비동기 웹 프레임워크
- **Uvicorn 0.24.0** - ASGI 서버

### 데이터베이스
- **SQLAlchemy 2.0.23** - ORM
- **Alembic 1.12.1** - 마이그레이션 도구
- **SQLite** - 경량 데이터베이스

### 검증 및 보안
- **Pydantic 2.5.0** - 데이터 검증
- **python-jose** - JWT 토큰
- **passlib** - 비밀번호 해싱

### HTTP 및 파일
- **httpx** - HTTP 클라이언트 (Lemon Squeezy API)
- **python-multipart** - 파일 업로드
- **aiofiles** - 비동기 파일 I/O

### 이메일
- **aiosmtplib** - 비동기 SMTP 클라이언트

## 🔐 환경 변수 (.env)

```env
# Database
DATABASE_URL=sqlite:///./data/qr_platform.db

# Lemon Squeezy
LEMON_SQUEEZY_API_KEY=your_api_key_here
LEMON_SQUEEZY_STORE_ID=your_store_id_here
LEMON_SQUEEZY_WEBHOOK_SECRET=your_webhook_secret_here

# Email (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
FROM_EMAIL=your_email@gmail.com
FROM_NAME=3D QR Platform

# Security
SECRET_KEY=your_jwt_secret_key_here_use_openssl_rand_hex_32
DOWNLOAD_TOKEN_EXPIRE_HOURS=72

# Storage
STORAGE_PATH=./storage/orders
STATIC_PATH=./static

# CORS
CORS_ORIGINS=http://localhost:5173,https://your-frontend-domain.com

# Cloudflare Tunnel
PUBLIC_URL=https://your-tunnel-domain.com
```

## 🚀 실행 방법

### 개발 환경 설정

```bash
# 1. 가상환경 생성 및 활성화
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 의존성 설치
pip install -r requirements.txt

# 3. 환경변수 설정
cp .env.example .env
# .env 파일 편집 (API 키, 이메일 설정 등)

# 4. 개발 서버 실행
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

서버 실행 후:
- API 문서: http://localhost:8000/docs (Swagger UI)
- ReDoc: http://localhost:8000/redoc

### 프로덕션 배포 (라즈베리파이 + Cloudflare Tunnel)

#### 1. 라즈베리파이 설정

```bash
# Python 가상환경 설정
cd /home/pi/3D_QR_backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 환경변수 설정
nano .env
```

#### 2. systemd 서비스 등록

**/etc/systemd/system/qr-backend.service**
```ini
[Unit]
Description=QR Backend FastAPI
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/3D_QR_backend
Environment="PATH=/home/pi/3D_QR_backend/venv/bin"
ExecStart=/home/pi/3D_QR_backend/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable qr-backend
sudo systemctl start qr-backend
sudo systemctl status qr-backend
```

#### 3. Cloudflare Tunnel 설정

```bash
# Cloudflare Tunnel 설치 (ARM64)
curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-arm64 -o cloudflared
sudo mv cloudflared /usr/local/bin/
sudo chmod +x /usr/local/bin/cloudflared

# 터널 생성
cloudflared tunnel login
cloudflared tunnel create qr-backend
cloudflared tunnel route dns qr-backend api.your-domain.com
```

**~/.cloudflared/config.yml**
```yaml
tunnel: qr-backend
credentials-file: /home/pi/.cloudflared/qr-backend.json

ingress:
  - hostname: api.your-domain.com
    service: http://localhost:8000
  - service: http_status:404
```

**/etc/systemd/system/cloudflared.service**
```ini
[Unit]
Description=Cloudflare Tunnel
After=network.target

[Service]
Type=simple
User=pi
ExecStart=/usr/local/bin/cloudflared tunnel --config /home/pi/.cloudflared/config.yml run
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable cloudflared
sudo systemctl start cloudflared
sudo systemctl status cloudflared
```

## 📋 TODO (미구현 기능)

### Phase 1: 파일 처리
- [ ] `app/services/storage.py` 구현
  - [ ] `save_order_stl()` - STL 파일 저장
  - [ ] `save_order_info()` - 주문 정보 JSON 저장
  - [ ] `get_order_file()` - 파일 조회

### Phase 2: 인증 및 토큰
- [ ] `app/utils/token.py` 구현
  - [ ] `generate_download_token()` - JWT 생성
  - [ ] `verify_download_token()` - JWT 검증

### Phase 3: 결제 연동
- [ ] `app/services/payment.py` 구현
  - [ ] `create_checkout_url()` - Lemon Squeezy 결제 URL 생성
  - [ ] `verify_webhook_signature()` - 웹훅 서명 검증

### Phase 4: 이메일
- [ ] `app/services/email.py` 구현
  - [ ] `send_download_link()` - 다운로드 링크 이메일 발송
  - [ ] 이메일 템플릿 작성

### Phase 5: 라우터 완성
- [ ] `app/routers/orders.py` - 주문 생성 로직 완성
- [ ] `app/routers/webhooks.py` - 웹훅 처리 로직 완성
- [ ] `app/routers/downloads.py` - 다운로드 로직 완성

### Phase 6: 데이터베이스
- [ ] Alembic 마이그레이션 설정
- [ ] 초기 거치대 데이터 시딩

### Phase 7: 테스트 및 모니터링
- [ ] 단위 테스트 작성
- [ ] 통합 테스트 작성
- [ ] 로깅 설정
- [ ] 모니터링 설정

## 📝 데이터 흐름

```
1. 프론트엔드
   ↓ POST /api/orders (STL + 주문 정보)

2. 백엔드 (app/routers/orders.py)
   ↓ storage.save_order_stl()
   ↓ DB.create(order)
   ↓ payment.create_checkout_url()
   ↓ return {order_uuid, payment_url}

3. 사용자 → Lemon Squeezy 결제

4. Lemon Squeezy
   ↓ POST /api/webhooks/lemonsqueezy

5. 백엔드 (app/routers/webhooks.py)
   ↓ payment.verify_webhook_signature()
   ↓ DB.update(order.status = "paid")
   ↓ token.generate_download_token()
   ↓ email.send_download_link()

6. 사용자 → 이메일 링크 클릭
   ↓ GET /api/downloads/{order_uuid}/{token}

7. 백엔드 (app/routers/downloads.py)
   ↓ token.verify_download_token()
   ↓ FileResponse(stl_file)
```

## 🔍 디버깅

```bash
# 로그 확인
sudo journalctl -u qr-backend -f

# Cloudflare Tunnel 로그
sudo journalctl -u cloudflared -f

# DB 확인
sqlite3 data/qr_platform.db
sqlite> .tables
sqlite> SELECT * FROM orders;
```

## 📚 참고 문서

- FastAPI: https://fastapi.tiangolo.com/
- SQLAlchemy: https://docs.sqlalchemy.org/
- Lemon Squeezy API: https://docs.lemonsqueezy.com/api
- Cloudflare Tunnel: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/
